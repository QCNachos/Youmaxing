'use client';

// The dashboard now uses the immersive layout with:
// - AvatarHero: Large 3D avatar section for aspect visualization
// - AIConversation: AI chat interface replacing the greeting/trends
// 
// This page intentionally renders nothing as all content is in the layout.
// The AspectRenderer is no longer needed since aspects are controlled via the 3D menu.

export default function DashboardPage() {
  return null;
}
